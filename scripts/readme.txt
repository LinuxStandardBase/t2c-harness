This directory contains sample scripts that can be used to generate and build 
C/C++ code of tests from a suite, generate code of the tests without actually
building it, run the tests and perform clean up.

The scripts performing these operations for a particular test suite should be
found in this test suite rather than here.
